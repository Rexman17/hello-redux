// 2 args to a reducer - state and an action
export default (state) => {
  return state // default is to return state if there's no action
  

}


// for a redux reducer, you always return the new state of your app
